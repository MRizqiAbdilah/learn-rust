struct NodeIot {
    pub id: u32,
    pub aktif: bool,
}

/// Instance struct NodeIot
///
/// # Contoh Penggunaan Struct
/// ```
/// let node = NodeIot{
///    id: 1,
///    aktif: true
/// };
/// assert_eq!(node.id, 1);
/// ```

fn main() {
    // Instance Here
}
